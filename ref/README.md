# ref/ — Reference Implementations

This directory contains reference implementations and design examples.

**These files are read-only reference material.**

Do not modify them — they serve as examples and may be used to compare implementation approaches. The active code is in:

- `agent_demo/` — Tauri desktop app + agent-core Rust library
- `coral-server/` — Axum REST API
- `packages/` — TypeScript packages (agent-core-ts, sdk)
